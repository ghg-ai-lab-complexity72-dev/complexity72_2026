Each folder represents a data variable from the dataset (emissions, growth rates,...) and each folder contains the following information in the form of a simple .csv file:

- lat,lon,variable

Where lat and lon are latitute and the longitude of the datapoint
